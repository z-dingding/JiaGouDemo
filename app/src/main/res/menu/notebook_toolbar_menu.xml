<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:osc="http://schemas.android.com/apk/res-auto">

    <!-- app:showAsAction="always"  always：使菜单项一直显示在ToolBar上。
      ifRoom：如果有足够的空间，这个值会使菜单项显示在ToolBar上。
      never：使菜单项永远都不出现在ToolBar上,在…的子项中显示。
      withText：使菜单项和它的图标，菜单文本一起显示。-->
    <!--使用app命名空间是为了兼容低版本的系统-->

    <item
        android:id="@+id/notebook_clearall"
        android:title="清空所有记录"
        android:visible="true"
        osc:showAsAction="never" />
</menu>